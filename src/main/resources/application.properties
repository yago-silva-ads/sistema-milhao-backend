# SISTEMA MILHAO - Backend
# Em produção (Railway), essas variáveis vêm do ambiente.
# Em local, usa os valores padrão abaixo.

# MySQL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/sistema_milhao?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USER:root}
spring.datasource.password=${DATABASE_PASSWORD:18m27j24Yag0;}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USER:bxtnaldo0z@gmail.com}
spring.mail.password=${GMAIL_APP_PASSWORD:qdjm chvb nzfl trnv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Mercado Pago
mercadopago.access-token=${MP_ACCESS_TOKEN:APP_USR-3264013058241106-072301-56d8c0874c17639dcf61e817f513bd59-2063934139}

# Server
server.port=${PORT:8080}
